import type { CharacterData } from 'lib/autobattle/types'

// Mydei is a Destruction DPS without a separate memo entity. His "Vendetta" state and HP-to-
// damage conversion happen entirely on his primary actor. v1 uses default energy mechanics
// (the in-game charged-skill conversion is not differentiated here).
export const MydeiData: CharacterData = {}
